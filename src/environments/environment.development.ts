export const environment = {
  production: true,
  apiUrl: process.env['API_URL'] || 'https://default-api-url.comdwadwadwa',  // Usar corchetes
  apiKey: process.env['API_KEY'] || 'default-api-keydwadwadwa'  // Usar corchetes
};
