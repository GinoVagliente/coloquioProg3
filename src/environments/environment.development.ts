export const environment = {
  production: true,
  apiUrl: process.env['API_URL'] || 'https://default-api-url.com',  // Usar corchetes
  apiKey: process.env['API_KEY'] || 'default-api-key'  // Usar corchetes
};
